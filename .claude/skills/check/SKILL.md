---
name: check
description: Run typecheck, lint, and tests. Fix any issues found.
---

Run typecheck, lint, and tests. Fix any issues found.

1. Run `npm run typecheck` — fix any TypeScript errors
2. Run `npm run lint` — fix any lint issues
3. Run `npm run test:unit` — fix any failing tests
4. Report a summary of what passed and what was fixed
